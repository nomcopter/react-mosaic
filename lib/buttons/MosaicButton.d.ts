import React from 'react';
export declare function createDefaultToolbarButton(title: string, className: string, onClick: (event: React.MouseEvent<any>) => any, text?: string): React.ReactElement<any>;
export interface MosaicButtonProps {
    onClick?: () => void;
}
